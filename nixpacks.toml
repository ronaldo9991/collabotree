[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['cd backend && npm ci --omit=dev --no-audit --no-fund']

[phases.build]
cmds = [
  'cd backend && npx prisma generate',
  'cd backend && npm run build'
]

[start]
cmd = 'cd backend && node ultra-minimal-start.js'
