# ============================================
# COLLABOTREE BACKEND ENVIRONMENT VARIABLES
# ============================================

# Environment (development, production, test)
NODE_ENV=production

# Port (Railway sets this automatically)
# PORT=4000

# Client Origin (optional for single Railway deployment)
# Leave empty for single deployment, or set to frontend URL for split deployment
CLIENT_ORIGIN=

# Database URL (Railway PostgreSQL provides this automatically)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/collabotree?schema=public

# JWT Secrets (REQUIRED - Generate strong secrets for production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ACCESS_SECRET=replace-me-with-a-strong-32-char-minimum-secret-key
JWT_REFRESH_SECRET=replace-me-with-another-strong-32-char-minimum-secret-key
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Password Hashing
BCRYPT_ROUNDS=12

# ============================================
# RAILWAY DEPLOYMENT NOTES:
# ============================================
# 1. Railway automatically provides: DATABASE_URL, PORT
# 2. Set JWT secrets in Railway dashboard
# 3. CLIENT_ORIGIN can be left empty for single deployment
# 4. For separate deployments, set CLIENT_ORIGIN to your frontend URL
